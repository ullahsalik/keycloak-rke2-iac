# keycloak-rke2-iac
Terraform is used as the single orchestration layer to bootstrap RKE2, install platform components via Helm, and deploy Keycloak in a fully reproducible manner suitable for both local and bare-metal environments.
